{
  "name": "log-manager",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy-stack": "docker stack deploy --compose-file=docker-compose.yml log-manager",
    "stop-stack": "docker service rm `docker service ls -q`",
    "update-consolidator": "cd consolidator && npm run build-docker && cd .. && docker service update --force log-manager_consolidator",
    "update-ingestor": "cd ingestor && npm run build-docker && cd .. && docker service update --force log-manager_ingestor"
  },
  "author": "Julien Duizabo",
  "license": "ISC"
}
